export const exportToExcel = (data: any[], fileName: string) => {
    console.log("Exporting to Excel:", fileName, data);
    // Placeholder for actual Excel export logic (e.g., using xlsx library)
    alert("Export to Excel started for " + fileName);
};

export const exportToPDF = (data: any[], fileName: string) => {
    console.log("Exporting to PDF:", fileName, data);
    // Placeholder for actual PDF export logic (e.g., using jspdf library)
    alert("Export to PDF started for " + fileName);
};
